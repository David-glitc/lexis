{
  "tasks": {
    "dev": "deno run --watch --allow-net --allow-env main.ts --port=8000",
    "start": "deno run --allow-net --allow-env main.ts --port=8000"
  },
  "compilerOptions": {
    "lib": ["deno.window", "dom", "esnext"],
    "strict": true
  }
}

